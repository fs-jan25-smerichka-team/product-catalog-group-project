import { Theme } from '@emotion/react';
import { SxProps } from '@mui/material';

export const fullSpecsSectionStyle: Record<string, SxProps<Theme>> = {
  divider: {
    marginTop: '16px',
  },

  specsContainer: {
    marginTop: '32px',
  },
};
