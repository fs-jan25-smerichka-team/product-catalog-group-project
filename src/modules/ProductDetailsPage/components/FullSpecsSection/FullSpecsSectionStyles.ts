import { Theme } from '@emotion/react';
import { SxProps } from '@mui/material';

export const techSpecsStyle: Record<string, SxProps<Theme>> = {
  divider: {
    marginTop: '16px',
  },

  contentContainer: {
    marginTop: '32px',
  },
};
